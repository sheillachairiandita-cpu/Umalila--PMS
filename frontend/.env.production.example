# Production — used by both pms.stayatumalila.com and booking.stayatumalila.com
# (same Cloudflare Pages project, hostname routing in App.jsx)

VITE_TENANT_SLUG=umalila
VITE_API_BASE_URL=https://api.stayatumalila.com

# Optional: force a surface when hostname detection is not enough (admin | booking | all)
# VITE_APP_HOST_MODE=all
