/** Configurable dashboard thresholds — tune per property or season without code changes. */
export const DASHBOARD_CONFIG = {
  /** Pending deposit triggers warning when it exceeds collected × this ratio (1.0 = 100%). */
  pendingDepositAlertThreshold: 1.0,
  /** Horizontal occupancy benchmark line on the Hospitality KPI trend chart. */
  occupancyTarget: 60,
};
